import React, { Fragment } from 'react';

export default (props: any) =>{


    // let runningTestsResults = useQuery(gql`
    //     query {
    //         getRunningTests @client
    //     }
    // `);
    // console.log({runningTestsResults})



    return (
        <Fragment>
            <h1>Test</h1>
        </Fragment>
    );
}
